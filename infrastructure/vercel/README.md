# Vercel Deployment Config

Place Vercel-specific config, environment templates, or serverless integration files here.

Recommended contents:

- `project.json` or `vercel.json`
- environment variable templates
- deployment notes for staging / production
