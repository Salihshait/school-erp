# CI Infrastructure

This directory is reserved for reusable CI scripts, shared templates, and automation helpers.

Possible additions:

- `deploy.sh` or `deploy.ps1`
- `scripts/` for build/release validation
- workflow snippets used by multiple pipelines
